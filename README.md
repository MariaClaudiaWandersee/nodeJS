Simples CRUD feito com NodeJS e Express, utilizando o postman para realizar as ações e o banco de dados MongoDB.

Para rodar:
- npm install
- npm run start:dev
- criar o .env com as variáveis para conexão do banco
